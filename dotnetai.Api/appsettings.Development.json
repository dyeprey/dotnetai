{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },

  // The Vite dev server. Only loaded when ASPNETCORE_ENVIRONMENT=Development, so these
  // origins cannot follow the app into production.
  "Cors": {
    "AllowedOrigins": [
      "http://localhost:5173",
      "http://127.0.0.1:5173"
    ]
  }
}
